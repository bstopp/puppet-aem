#!/bin/bash
#
# This script is generated by the AEM Puppet module. 
# Do not manually edit this file.
#
# This file manages the configuration of the run script.
#


PORT=<%= @resource[:port] %>
TYPE=<%= @resource[:type] %>
RUNMODES='<%= @resource[:runmodes].join(",") if @resource[:runmodes] %>'
SAMPLE_CONTENT='<%= "nosamplecontent" if @resource[:sample_content] == :false %>'
DEBUG_PORT=

<% if @resource[:context_root] %>
CONTEXT_ROOT='<%= @resource[:context_root] %>'
<% end %>

JVM_MEM_OPTS='<%= @resource[:jvm_mem_opts] %>'

DEFAULT_JVM_OPTS='-server -Djava.awt.headless=true'
JVM_OPTS='<%= @resource[:jvm_opts] %>'

SLING_PROPS='conf/sling.properties'

# Do we ever implement these?
#HOST=
#FILE_SIZE_LIMIT

# This doesn't ever seem to work
#LOG_LEVEL=
